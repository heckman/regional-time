Regional-time
=============

A command-line utility for macOS that, given a number of seconds since the Unix epoch and a latitude and longitude, prints the corresponding date and time for the location's timezone in the ISO 8601 format.
